try:
    import streamlit_authenticator as stauth
except ModuleNotFoundError:
    st.error("streamlit‑authenticator missing …")
    st.stop()

streamlit
pandas
plotly
fpdf
python-pptx
requests
statsmodels
openpyxl          # Excel export
# streamlit-authenticator   ← commented because you disabled auth
